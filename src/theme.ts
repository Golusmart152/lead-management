// Theme configuration for Tailwind CSS-based styling
// MUI theme has been removed in favor of Radix UI + Tailwind CSS

export const theme = {
  colors: {
    primary: {
      main: '#1976d2',
    },
    secondary: {
      main: '#dc004e',
    },
  },
};

// For backward compatibility, maintain the same export structure
export const muiTheme = theme;
